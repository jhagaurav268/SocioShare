import { LightningElement, api} from 'lwc';
export default class PostPageSuccess extends LightningElement {

    // @api parentId;

    // handleNewPost(event){
    //     console.log('new Post Method', this.parentId);
    //     var selectedEvent
    //      switch (this.parentId) {
    //         case 'fb':  selectedEvent = new CustomEvent('newpostevent', { detail: this.parentId});      
    //            break;
    //         case 'insta':  selectedEvent = new CustomEvent('newpostevent', { detail: this.parentId});
    //             break;
    //         case 'lnkdIn':  selectedEvent = new CustomEvent('newpostevent', { detail: this.parentId});
    //             break;
    //         default:
            
    //     }
    //     this.dispatchEvent(selectedEvent);


    // }

}